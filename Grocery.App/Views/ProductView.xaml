<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             x:Class="Grocery.App.Views.ProductView"
             Title="ProductView">

    <Shell.TitleView>
        <Grid>
            <Label Text="Producten" FontSize="Large" HorizontalOptions="Center" VerticalOptions="Center" />
        </Grid>
    </Shell.TitleView>

    <Border Stroke="#C49B33" StrokeThickness="4" Padding="10" Margin="10" HorizontalOptions="Center">
        <CollectionView ItemsSource="{Binding Products}" Margin="20" HorizontalOptions="Center">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
            </CollectionView.ItemsLayout>
            <CollectionView.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Text="Naam van het product" Grid.Column="0" FontAttributes="Bold"/>
                    <Label Text="Prijs" Grid.Column="1" FontAttributes="Bold" HorizontalTextAlignment="Start"/>
                    <Label Text="Voorraad" Grid.Column="2" FontAttributes="Bold" HorizontalTextAlignment="End"/>
                    <Label Text="THT datum" Grid.Column="3" FontAttributes="Bold" HorizontalTextAlignment="End"/>
                </Grid>
            </CollectionView.Header>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Label Grid.Column="0" Text="{Binding Name}" VerticalOptions="Center"/>
                        <Label Grid.Column="1" Text="{Binding Price, StringFormat='€{0:F2}'}" VerticalOptions="Center"/>
                        <Label Grid.Column="2" VerticalOptions="Center" HorizontalOptions="End">
                            <Label.Text>
                                <Binding Path="Stock" StringFormat="{}{0:F0} op voorraad"/>
                            </Label.Text>
                            <Label.Triggers>
                                <DataTrigger TargetType="Label" Binding="{Binding Stock}" Value="0">
                                    <Setter Property="Text" Value="niet op voorraad" />
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>

                        <Label Grid.Column="3" Text="{Binding ShelfLife}" HorizontalOptions="End" VerticalOptions="Center"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Border>
</ContentPage>
